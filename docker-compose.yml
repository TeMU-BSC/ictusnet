services:

  mongo:
    image: mongo
    container_name: ictusnet_mongo
    ports:
      - 27017-27019:27017-27019
    volumes:
      - ./database/mongodb:/data/db

  # ictusnet-ctakes:
  #   image: bsctemu/ictusnet
  #   container_name: ictusnet_ctakes

  node:
    container_name: ictusnet_node
    build: ./backend
    ports:
      - 3000:3000
    volumes:
      - ./backend:/app
      - /var/run/docker.sock:/var/run/docker.sock
    privileged: true
    environment:
      - MONGODB_HOST=mongo
    command: npm run dev

  angular:
    container_name: ictusnet_angular
    build: ./frontend
    ports:
      - 4200:4200
    volumes:
      - ./frontend:/app
    command: npm run dev
